interface:
  display_name: "Starter Rule Sync"
  short_description: "Route old rule-sync prompts"
  default_prompt: "Use $starter-rule-sync only to route old rule-sync prompts to $starter-rule-report or $starter-rule-import."
