interface:
  display_name: "GitHub CI Diagnostics"
  short_description: "Inspect failing PR checks"
  default_prompt: "Use $gh-fix-ci to inspect failing GitHub PR checks and summarize actionable log context."
