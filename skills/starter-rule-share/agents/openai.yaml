interface:
  display_name: "starter-rule-share"
  short_description: "Share starter rules to approved projects"
  default_prompt: "Use $starter-rule-share to propose or run guarded one-run sharing for approved downstream projects."
