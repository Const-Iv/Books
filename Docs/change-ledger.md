# Change Ledger

Human-readable snapshot of meaningful project changes.

## Entries

- `20260416-runnable-baseline` — Runnable Node/npm process baseline (`done`)
- `20260429-starter-rule-share` — Approval-safe outbound starter rule sharing (`done`)
- `20260505-echo-testing-gate` — Echo-testing gate for unknown root technology during product bootstrap (`wip`)
- `<YYYYMMDD-HHMMSS>-<task-id>` — `<change title>` (`wip|done|released`)
